{
  "name": "arduino-mqqt-api",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint lib/Board.ts || true",
    "build": "rm -r lib/* && babel src -d lib",
    "test": "jasmine || true",
    "watch": ""
  },
  "dependencies": {
    "mqtt": "^1.7.4",
    "q": "^1.4.1"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "eslint": "^2.5.1",
    "jasmine": "^2.4.1"
  }
}
